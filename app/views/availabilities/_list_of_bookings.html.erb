<div class="row calendar calendar-owner">
  <div class="col-xs-12">
    <h3> Les visites programmées</h3>
    <% bookings.each do |booking|%>
      <%= I18n.l(booking.first.availability.start_time, format: "%A %d %B %H:%M") %> <br>
    <% end%>
  </div>
</div>

