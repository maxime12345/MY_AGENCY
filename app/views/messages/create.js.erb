function refreshForm(innerHTML) {
  console.log("je suis dans refreshForm")
  const newMessageForm = document.getElementById('message_content');
  newMessageForm.innerHTML = innerHTML;
}

function addMessage(messageHTML) {
  console.log("je suis dans addMessage")
  const conversation = document.getElementById('conversation');
  conversation.insertAdjacentHTML('beforeend', messageHTML);
}

<% if @message.errors.any? %>
  refreshForm('<%= j render "messages/form", candidacy: @candidacy, message: @message %>');
<% else %>
  addMessage('<%= j render "messages/index", message: @message %>');
  refreshForm('<%= j render "messages/form", , candidacy: @candidacy, message: Message.new %>');
<% end %>
