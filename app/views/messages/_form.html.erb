<%= simple_form_for([ candidacy, message ], remote: true) do |form| %>
  <%= form.input :content, label: "", as: :text %>
  <%= button_tag(type: 'submit', class: "btn btn-primary pull-right") do %>
    <i class="fas fa-paper-plane"></i>
    Envoyer
  <% end %>
<% end %>
