<%= simple_form_for(candidacy, method: :put) do |f| %>
  <%= f.hidden_field :status, value: status %>
  <% if status == "Dossier archivé" %>
    <%= f.button :button do %>
      <i class="fas fa-archive"> </i>
    <% end %>
  <% else %>
    <%= f.button :submit, 'Approuver', class: "btn btn-sm btn-primary accept-candidat" %>
  <% end %>
<% end %>


